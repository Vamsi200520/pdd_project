name: CI Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  api-tests:
    name: API-100 Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: Install Backend Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r asthma-backend/requirements.txt
          pip install pytest fastapi httpx python-dotenv passlib bcrypt==3.2.2 python-multipart email-validator "python-jose[cryptography]"

      - name: Run 100 API Tests
        run: |
          pytest tests/test_api.py -v

  selenium-tests:
    name: Selenium-300 Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Selenium and Pytest
        run: |
          python -m pip install --upgrade pip
          pip install selenium pytest

      - name: Run 300 Selenium Web Tests
        run: |
          pytest tests/test_selenium.py -v

  appium-tests:
    name: Appium-300 Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Pytest
        run: |
          python -m pip install --upgrade pip
          pip install pytest

      - name: Run 300 Appium Mobile Tests
        run: |
          pytest tests/test_appium.py -v

  vulnerability-scan:
    name: Vulnerability-100 Scan
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Scanner Tools
        run: |
          python -m pip install --upgrade pip
          pip install pip-audit

      - name: Run Vulnerability Scan
        run: |
          python tests/vulnerability_scan.py

      - name: Upload Scan Report
        uses: actions/upload-artifact@v4
        with:
          name: vulnerability-report
          path: reports/vulnerability_report.json

  load-tests:
    name: Load-300 Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: Install Backend Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r asthma-backend/requirements.txt
          pip install pytest fastapi httpx python-dotenv passlib bcrypt==3.2.2 python-multipart email-validator "python-jose[cryptography]"

      - name: Run 300 Load Tests
        run: |
          pytest tests/test_load.py -v

  threshold-check:
    name: Threshold-100 Check
    runs-on: ubuntu-latest
    needs: [api-tests, selenium-tests, appium-tests, vulnerability-scan, load-tests]
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Download Scan Report
        uses: actions/download-artifact@v4
        with:
          name: vulnerability-report
          path: reports

      - name: Install Report Generator Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pandas openpyxl

      - name: Generate Excel Reports
        run: |
          python tests/generate_excel_reports.py reports

      - name: Run Quality Gate Check
        run: |
          python tests/threshold_check.py

      - name: Upload API Tests Excel Report
        uses: actions/upload-artifact@v4
        with:
          name: api-tests-report
          path: reports/api_tests_report.xlsx

      - name: Upload Selenium Tests Excel Report
        uses: actions/upload-artifact@v4
        with:
          name: selenium-tests-report
          path: reports/selenium_tests_report.xlsx

      - name: Upload Appium Tests Excel Report
        uses: actions/upload-artifact@v4
        with:
          name: appium-tests-report
          path: reports/appium_tests_report.xlsx

      - name: Upload Load Tests Excel Report
        uses: actions/upload-artifact@v4
        with:
          name: load-tests-report
          path: reports/load_tests_report.xlsx

      - name: Upload Vulnerability Excel Report
        uses: actions/upload-artifact@v4
        with:
          name: vulnerability-excel-report
          path: reports/vulnerability_report.xlsx

      - name: Upload CI Summary Excel Report
        uses: actions/upload-artifact@v4
        with:
          name: ci-summary-report
          path: reports/ci_summary_report.xlsx
